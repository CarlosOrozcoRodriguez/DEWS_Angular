export class Libro {
    id?: any;//probalemente sea un int, pero no estoy seguro
    titulo?: string; //la ? significa que es opcional. no hacce falta poner tipo nulo? |null
    argumento?: string;
    enCatalogo?: boolean;
  }